@model north.ViewModels.TerritoriesViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Record Details</h2>
<div>
    <div>
        <fieldset>
            <legend></legend>
            <table>
                <tr>
                    <td class="editor-label-bold">@Html.LabelFor(vm => vm.TerritoriesModel.TerritoryID):</td>
                    <td class="editor-label">@Html.DisplayFor(vm => vm.TerritoriesModel.TerritoryID)</td>
                </tr>
                <tr>
                    <td class="editor-label-bold">@Html.LabelFor(vm => vm.TerritoriesModel.TerritoryDescription):</td>
                    <td class="editor-label">@Html.DisplayFor(vm => vm.TerritoriesModel.TerritoryDescription)</td>
                </tr>
                <tr>
                    <td class="editor-label-bold">@Html.LabelFor(vm => vm.TerritoriesModel.RegionID):</td>
                    <td class="editor-field">@Html.DisplayFor(vm => vm.TerritoriesModel.RegionID)</td>
                </tr>
                <tr>
                    <td colspan="2">
                        <br />

                        @if (Request.UrlReferrer != null)
                        {
                            <a href="@Request.UrlReferrer.ToString()">Return</a>
                        }
                        else
                        {
                            <a href="/Home/Index">Return</a>
                        }
                    </td>
                </tr>
            </table>
        </fieldset>
    </div>
</div>
